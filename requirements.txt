# 貼り付けモードの解析は Python 標準ライブラリ ast のみ（外部依存なし）。
# st.html(unsafe_allow_javascript=...) を使うため streamlit は 1.50 以上を要求する。
streamlit>=1.50

# フォルダ解析モード（カバレッジ×複雑度）で実行時に使う。
# pytest でテストを実行し、pytest-cov/coverage で行・分岐カバレッジを測り、
# radon で複雑度を測って突き合わせる（cross_check.py が subprocess で呼ぶ）。
pytest>=8.0
pytest-cov>=5.0
coverage>=7.0
radon>=6.0

# ファイル横断の静的コールグラフ（callgraph.py が subprocess で `python -m pyan` を呼ぶ）。
# 別ファイルへの呼び出し・同名関数をスコープ解決で辿る。図の書き出し（SVG）には別途
# OS の Graphviz が必要（`sudo apt install graphviz` / `brew install graphviz`）。
# ブラウザ上の図表示（st.graphviz_chart）だけなら Graphviz の導入は不要。
pyan3>=1.2.0

# 実行経路（動的）の裏取り: テストを cProfile（標準ライブラリ）下で実行し、その
# pstats を gprof2dot で呼び出し回数つきのコールグラフに変換する（callgraph.py）。
# 静的では辿れない動的 dispatch（visitor の self.visit() 等）を実行経路で裏取りする。
gprof2dot>=2022.7.29
